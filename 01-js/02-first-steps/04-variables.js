"use strict"

let message = 'Kolbasa';
let $ = 'dollar';  // Не просмотреть в консоли Файрфокса. На $ завязана какая-то встроенная функция
let _ = 'underscore';

const obj = {};
obj.age = 12;  // Работает

const str = "Hello";
// str = "Hi";  // Не работает


// Задачи
let admin;
let name;

name = 'John';
admin = name;

alert(admin);
